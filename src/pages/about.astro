---
import '../styles/global.css';
import { Header } from '../components/layout/Header';
import { Footer } from '../components/layout/Footer';
import { PageDetail } from '../components/pages/PageDetail';
import { wpApi } from '../lib/wordpress';
import logoImage from '../assets/images/Tu-San-Agustin_Logo.png';

// Fetch "About" page from WordPress
let page = null;
try {
  page = await wpApi.getPage('about') || await wpApi.getPage('sobre-nosotros') || await wpApi.getPage('acerca-de');
} catch (error) {
  console.error('Error fetching about page:', error);
}

// Fallback content if no page found
const fallbackPage = {
  id: 0,
  title: { rendered: 'Acerca de Tu San Agustín' },
  content: { 
    rendered: `
      <p>Somos Tu San Agustín, la empresa líder en operación de eventos con más de 35 años de experiencia. Ofrecemos soluciones integrales y personalizadas para cualquier tipo de evento.</p>
      
      <h2>Nuestra Historia</h2>
      <p>Con más de tres décadas en el mercado, hemos consolidado nuestra posición como líderes en la organización de eventos y turismo especializado en San Agustín, Huila.</p>
      
      <h2>Nuestra Misión</h2>
      <p>Crear experiencias únicas e inolvidables a través de eventos perfectamente planificados y ejecutados, superando siempre las expectativas de nuestros clientes.</p>
      
      <h2>Nuestros Valores</h2>
      <ul>
        <li><strong>Excelencia:</strong> Buscamos la perfección en cada detalle</li>
        <li><strong>Innovación:</strong> Implementamos las últimas tendencias</li>
        <li><strong>Compromiso:</strong> Dedicación total con cada proyecto</li>
        <li><strong>Confiabilidad:</strong> Cumplimos lo que prometemos</li>
      </ul>
    ` 
  },
  excerpt: { rendered: 'Conoce más sobre Tu San Agustín, líderes en eventos y turismo.' },
  date: new Date().toISOString(),
  featured_media: 0,
  author: 1,
  _embedded: null
};
---

<html lang="es">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="generator" content={Astro.generator} />
    <title>Acerca de - Tu San Agustín</title>
    <meta name="description" content="Conoce más sobre Tu San Agustín, líderes en eventos y turismo con más de 35 años de experiencia." />
    <link rel="icon" type="image/x-icon" href="/favicon.ico" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="apple-touch-icon" href="/apple-touch-icon.png" />
    <link rel="manifest" href="/site.webmanifest" />
  </head>
  <body>
    <Header client:load logoUrl={logoImage.src} />
    
    <main class="min-h-screen bg-gray-50 py-12">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <PageDetail client:load page={page || fallbackPage} />
      </div>
    </main>
    
    <Footer client:load />
  </body>
</html>